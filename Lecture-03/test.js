const  furniture = ['Table','Chairs','Couch'];
for(let i = 0 ; i <= furniture.length-1;i++){
    one_thing = furniture[i];
    for(let x = 0; x <= one_thing.length-1;x++){
        console.log(one_thing[x]);
    }
};